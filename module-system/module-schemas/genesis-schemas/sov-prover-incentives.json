{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ProverIncentivesConfig",
  "description": "Configuration of the prover incentives module. Specifies the minimum bond, the commitment to the allowed verifier method and a set of initial provers with their bonding amount.",
  "type": "object",
  "required": [
    "initial_provers",
    "minimum_bond",
    "proving_penalty"
  ],
  "properties": {
    "initial_provers": {
      "description": "A list of initial provers and their bonded amount.",
      "type": "array",
      "items": {
        "type": "array",
        "items": [
          {
            "$ref": "#/definitions/Address"
          },
          {
            "type": "integer",
            "format": "uint64",
            "minimum": 0.0
          }
        ],
        "maxItems": 2,
        "minItems": 2
      }
    },
    "minimum_bond": {
      "description": "The minimum bond for a prover.",
      "type": "integer",
      "format": "uint64",
      "minimum": 0.0
    },
    "proving_penalty": {
      "description": "A penalty for provers who submit a proof for transitions that were already proven",
      "type": "integer",
      "format": "uint64",
      "minimum": 0.0
    }
  },
  "definitions": {
    "Address": {
      "description": "Address",
      "type": "string",
      "pattern": "^sov1[a-zA-Z0-9]+$"
    }
  }
}
