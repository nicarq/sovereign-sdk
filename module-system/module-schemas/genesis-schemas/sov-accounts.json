{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AccountConfig",
  "description": "Initial configuration for sov-accounts module.",
  "type": "object",
  "required": [
    "accounts"
  ],
  "properties": {
    "accounts": {
      "description": "Accounts to initialize the rollup.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/AccountData_for_Address"
      }
    }
  },
  "definitions": {
    "AccountData_for_Address": {
      "description": "Account data for the genesis.",
      "type": "object",
      "required": [
        "address",
        "pub_key_hash"
      ],
      "properties": {
        "address": {
          "description": "Address of the account.",
          "allOf": [
            {
              "$ref": "#/definitions/Address"
            }
          ]
        },
        "pub_key_hash": {
          "description": "Hash of the public key.",
          "type": "string"
        }
      }
    },
    "Address": {
      "description": "Address",
      "type": "string",
      "pattern": "^sov1[a-zA-Z0-9]+$"
    }
  }
}
