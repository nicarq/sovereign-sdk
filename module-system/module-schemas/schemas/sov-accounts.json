{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CallMessage",
  "description": "Represents the available call messages for interacting with the sov-accounts module.",
  "oneOf": [
    {
      "description": "Updates credential id for the corresponding Account.",
      "type": "object",
      "required": [
        "UpdatePublicKey"
      ],
      "properties": {
        "UpdatePublicKey": {
          "$ref": "#/definitions/CredentialId"
        }
      },
      "additionalProperties": false
    }
  ],
  "definitions": {
    "CredentialId": {
      "description": "Type that represents an identifier for an authorizer of the transaction.",
      "type": "array",
      "items": {
        "type": "integer",
        "format": "uint8",
        "minimum": 0.0
      },
      "maxItems": 32,
      "minItems": 32
    }
  }
}
